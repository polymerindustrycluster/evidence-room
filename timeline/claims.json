{
 "data": {
  "h": "heritage.json",
  "t": "timeline.json"
 },
 "claims": [
  {
   "id": "tl-heritage-count",
   "text": "31 proven events, the first dated 1898 and the last 2012: 19 that changed the region's capacity and 12 scientific firsts (hero standfirst, heritage-band lede, hero stat card). [2026-08-27: sentence moved from the old lede into the rewritten lede + prologue sub; assertion unchanged.] [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.] [2026-08-29: was 32 and 13. Two register rows carried ONE event in identical words, counted once as heritage and once as discovery; the discovery row is merged away. The header counts are now checked against the rows rather than trusted, because it was the header, not the rows, that the page had been printing.]",
   "source": "web/timeline/data/heritage.json, built by tools/build-heritage-web.py from the NEO Polymer Wins register (INDEX.md heritage + discoveries collections)",
   "assert": "(lambda E, c: len(E) == 31 and c['shown'] == len(E) and c['heritage'] == len([e for e in E if e['collection'] == 'heritage']) == 19 and c['discoveries'] == len([e for e in E if e['collection'] == 'discoveries']) == 12 and c['firstYear'] == min(e['year'] for e in E) == 1898 and c['lastYear'] == max(e['year'] for e in E) == 2012)(D['h']['events'], D['h']['meta']['counts'])",
   "actual": "(len(D['h']['events']), D['h']['meta']['counts']['shown'], D['h']['meta']['counts']['heritage'], D['h']['meta']['counts']['discoveries'], D['h']['meta']['counts']['firstYear'], D['h']['meta']['counts']['lastYear'])",
   "falsified_if": "The register gains or loses a PROVEN heritage or discovery row, a row's label changes, or the header counts drift from the rows they claim to count. The lede, the prologue heading, the topline tile and the methods paragraph are all written from the rows at load time, so the static numbers here must move with them \u2014 this claim is what forces that."
  },
  {
   "id": "tl-heritage-no-double-count",
   "text": "No two shipped heritage rows are the same event: 31 rows, 31 distinct facts (hero standfirst count, heritage-band lede split, heritage figure source line). [2026-08-29: NEW. A reader found 'Jan 1963 Eric Baer founds the first standalone U.S. polymer department at Case' and '1963 Case Institute first standalone US polymer department' carrying the IDENTICAL register sentence, one in each collection, and the same overlap on the 1965 Kent institute. The count was a count of ROWS and nobody had checked it was a count of EVENTS.]",
   "source": "heritage.json: the register sentence each row prints, compared against every other row's; meta.merged and meta.narrowed record what was resolved and why",
   "assert": "(lambda L: all(not (L[i] in L[j] or L[j] in L[i]) for i in range(len(L)) for j in range(len(L)) if i < j) and len(set(s[:60] for s in L)) == len(L))([e['oneLiner'] for e in D['h']['events']]) and len(D['h']['meta']['merged']) == 1 and D['h']['meta']['merged'][0]['id'] == 'D-003' and D['h']['meta']['merged'][0]['into'] == 'H-007' and not any(e['id'] == 'D-003' for e in D['h']['events']) and len(D['h']['meta']['narrowed']) == 1 and D['h']['meta']['narrowed'][0]['id'] == 'D-004' and one(e for e in D['h']['events'] if e['id'] == 'D-004')['year'] == 1994 and 'trustees authorized' not in one(e for e in D['h']['events'] if e['id'] == 'D-004')['oneLiner']",
   "actual": "(lambda E, L: [(E[i]['id'], E[j]['id']) for i in range(len(L)) for j in range(len(L)) if i < j and (L[i] in L[j] or L[j] in L[i] or L[i][:60] == L[j][:60])])(D['h']['events'], [e['oneLiner'] for e in D['h']['events']])",
   "falsified_if": "One shipped row's register sentence turns up inside another's, two rows open with the same 60 characters, the merged row reappears in the events array, or D-004 drifts back to carrying H-008's founding sentence. A count of rows is only a count of events while no two rows are the same event, and that is a property nothing else on this page checked."
  },
  {
   "id": "tl-heritage-proven-only",
   "text": "PIC's register can prove 19 capacity events and 12 firsts, each against a named public source; 5 claimed rows are withheld (heritage-band lede, figure source line, methodology box). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.] [2026-08-29: 13 became 12: one discovery row recorded the same event as a heritage row in the same words and is merged into it. The proven/withheld mechanism this pins is unchanged.]",
   "source": "heritage.json: every emitted event carries label PROVEN; the builder drops every other label and records the drops",
   "assert": "all(e['label'] == 'PROVEN' for e in D['h']['events']) and D['h']['meta']['counts']['dropped'].get('label:CLAIMED', 0) == 5 and D['h']['meta']['counts']['dropped'].get('label:ASPIRATIONAL', 0) == 0 and len(D['h']['meta']['withheld']) == 5 and all(w['label'] != 'PROVEN' for w in D['h']['meta']['withheld'])",
   "actual": "(sorted(set(e['label'] for e in D['h']['events'])), D['h']['meta']['counts']['dropped'], [w['id'] for w in D['h']['meta']['withheld']])",
   "falsified_if": "Any non-PROVEN row is emitted, or the withheld count stops matching the drop count. Five CLAIMED rows are held today (H-001 Goodrich 1870, H-004 UA lab 1909, H-006 wartime GR-S, H-008c Morton 1948, H-015 Promerus 2020); if a later wave proves one, the number here changes and the sentence still holds \u2014 the assertion pins the mechanism, the count is the current reading."
  },
  {
   "id": "tl-heritage-every-mark-has-source",
   "text": "Every mark below opens its source (heritage-band lede). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "heritage.json: sources per event, http(s) only after the builder strips vault references",
   "assert": "all(any(s['url'] and s['url'].startswith('http') for s in e['sources']) for e in D['h']['events']) and not any('vault' in (s['note'] or '').lower() or '.md' in (s['note'] or '') for e in D['h']['events'] for s in e['sources'])",
   "actual": "(sum(1 for e in D['h']['events'] if any(s['url'] for s in e['sources'])), len(D['h']['events']), D['h']['meta']['counts']['sourcesStripped'])",
   "falsified_if": "A row is emitted with no public URL, or an internal source reference (a vault path, a .md file) leaks into a source note. The register is internal; only URLs and plain bibliographic lines may travel."
  },
  {
   "id": "tl-heritage-wartime-withheld",
   "text": "Rows the register labels claimed, where a date or a figure is recorded two ways, are withheld rather than shown with a caveat; the wartime synthetic-rubber program, its tonnage recorded four ways, is one of them. [2026-08-27: sentence moved from the prologue sub into the methods limits list; assertion unchanged.] It is now published in the generated methodology box under Limitations, rewritten without em-dashes for the house prose ban. [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "NEO-POLYMER-WINS INDEX.md: H-006 (1942\u201345 GR-S program) is CLAIMED because wartime tonnage is recorded four different ways; heritage.json meta.withheld carries the id",
   "assert": "any(w['id'] == 'H-006' and w['label'] == 'CLAIMED' for w in D['h']['meta']['withheld']) and not any(e['id'] == 'H-006' for e in D['h']['events']) and not any('800,000' in e['oneLiner'] or '920,000' in e['oneLiner'] for e in D['h']['events'])",
   "actual": "[w for w in D['h']['meta']['withheld'] if w['id'] == 'H-006']",
   "falsified_if": "H-006 is promoted to PROVEN (then this sentence must be rewritten, not the assertion), or a wartime tonnage figure appears in any emitted one-liner. The register's rule: four measures, no single number is honest, print none."
  },
  {
   "id": "tl-heritage-title-count-in-methods",
   "text": "Of its 37 heritage and discovery rows, 32 are labeled proven; 1 of those records the same event as another row, in the same words, and is merged into it, so 31 appear here; 5 labeled claimed are withheld. 3 internal source references were stripped in the build. (methodology box, Data sources.) [2026-08-29: the reconciliation used to be two parts and is now three, because the merge is a third way a register row can fail to reach the page and a reader adding 31 and 5 would otherwise come up one short of 37.]",
   "source": "heritage.json meta.counts (methods paragraph is written from these at load time)",
   "assert": "D['h']['meta']['counts']['shown'] + len(D['h']['meta']['merged']) + D['h']['meta']['counts']['dropped'].get('label:CLAIMED', 0) == 37 and D['h']['meta']['counts']['merged'] == len(D['h']['meta']['merged']) == 1 and D['h']['meta']['counts']['sourcesStripped'] == 3",
   "actual": "(D['h']['meta']['counts']['shown'], len(D['h']['meta']['merged']), D['h']['meta']['counts']['dropped'], D['h']['meta']['counts']['sourcesStripped'])",
   "falsified_if": "The register's heritage + discoveries collections change size, a row is merged or un-merged without the count moving, or a re-run of the builder strips a different number of internal references. The methods paragraph is generated, so it cannot drift from the data; this claim is what catches this file drifting from both."
  },
  {
   "id": "tl-heritage-gap",
   "text": "The register holds no NEW proven row after 2012 and nothing at all after 2016, so the silence before the October 2023 designation is seven years, not eleven (heritage-band figure title, the drawn sixth block, heritage aria, methodology Limitations). [2026-08-29: REPLACES the claim 'the proven record runs 1898 to 2012, then goes quiet until the designation, eleven years on.' That finding did not survive its own bucketing. The eleven years was 2023 minus the last row's DATE, and it was drawn in a column that could not have been filled: the last era declared itself 2000 to 2019 while the column was labeled 2013 to 2023, so any row from 2013 to 2019 was assigned to the era and the column was empty by construction. With the boundary fixed, two rows are recorded as running into that window, an NSF center renewed to 2016 among them. What is left is true and smaller, and it is stated at its real size.] [2026-09-01: the silence is now attributed. Two rendered strings said 'the register records nothing at all' after the last covered year, and the register's own file refutes that: it holds H-015, dated 2020, labeled claimed and withheld here, and 29 rows in three other collections that this heritage view drops outright. The figure title above the chart had scoped it correctly to PROVEN rows all along; the aria labels, the chart's own footer and the methodology now say the same thing, and name what the register holds inside the window that this page does not show.]",
   "source": "heritage.json: event years vs endYear (the last year each row's recorded span covers), the era boundaries, and the designation event in timeline.json",
   "assert": "(lambda E, des: max(e['year'] for e in E) == 2012 and D['h']['meta']['counts']['lastYear'] == max(e['year'] for e in E) and max(e['endYear'] for e in E) == 2016 and D['h']['meta']['counts']['lastCoveredYear'] == max(e['endYear'] for e in E) and max(e['endYear'] for e in E) > max(e['year'] for e in E) and all(e['endYear'] >= e['year'] for e in E) and des - max(e['endYear'] for e in E) == 7)(D['h']['events'], int(one(e['date'] for e in D['t']['events'] if e['date'].startswith('2023') and 'esignat' in e['title'])[:4])) and len([w for w in D['h']['meta']['withheld'] if 2013 <= int(str(w['year'])[:4]) <= 2023]) == 1 and one(w for w in D['h']['meta']['withheld'] if int(str(w['year'])[:4]) >= 2013)['id'] == 'H-015' and sum(v for k, v in D['h']['meta']['counts']['dropped'].items() if k.startswith('collection:')) == 29 and not any(e['year'] >= 2013 for e in D['h']['events'])",
   "actual": "(max(e['year'] for e in D['h']['events']), max(e['endYear'] for e in D['h']['events']), [(e['id'], e['dateDisplay'], e['endYear']) for e in D['h']['events'] if e['endYear'] > e['year']])",
   "falsified_if": "A proven row is dated after 2012, a recorded span is extended past 2016, or the designation moves. If the gap between the register's last coverage and the designation stops being seven years, the figure title, the block's own words and the Limitations paragraph are all wrong together, which is the point of pinning the two different last years side by side rather than only the one that made the better sentence. And it is falsified by any page string that attributes the silence to THE REGISTER rather than to the proven rows: the register is not empty in that window and this claim pins what is in it, so a rebuild cannot quietly restore the old wording. If H-015 is ever promoted to proven, the gap itself moves and every sentence here is rewritten rather than re-counted."
  },
  {
   "id": "tl-heritage-blocks-partition",
   "text": "The heritage strip's blocks tile the timeline without overlapping: each era ends where the next begins, the sixth block begins where the last era ends, and every row falls inside the block its year belongs to. [2026-08-29: NEW, and the reason the 'eleven years, nothing proven' claim was withdrawn. The last era ran to 2020 while the sixth block was typed 2013 to 2023, so 2013 to 2019 belonged to both and resolved to the era. The block could not fill from data whatever the register said, and an empty block and an unfillable one look identical on a page. The boundaries are now checked, so a silence has to be earned.]",
   "source": "heritage.json eras (from/to/label) and every event's year and era index",
   "assert": "all(D['h']['eras'][i]['to'] == D['h']['eras'][i + 1]['from'] for i in range(len(D['h']['eras']) - 1)) and all(D['h']['eras'][e['era']]['from'] <= e['year'] < D['h']['eras'][e['era']]['to'] for e in D['h']['events']) and all(x['label'] == str(x['from']) + '\u2013' + str(x['to'] - 1) for x in D['h']['eras'][1:]) and D['h']['eras'][-1]['to'] == 2013 and len([e for e in D['h']['events'] if e['year'] >= D['h']['eras'][-1]['to']]) == 0 and len([e for e in D['h']['events'] if e['endYear'] >= D['h']['eras'][-1]['to']]) == 2",
   "actual": "([(x['from'], x['to'], x['label']) for x in D['h']['eras']], [(e['id'], e['year'], e['endYear']) for e in D['h']['events'] if e['endYear'] >= D['h']['eras'][-1]['to']])",
   "falsified_if": "Two blocks overlap or leave a gap, a row's era index stops matching its year, an era's label stops matching its own bounds, or the number of rows running into the sixth block changes while the figure subtitle still says two. The chart draws that block's contents, so a wrong boundary is now a visibly wrong chart rather than a sentence nobody can check."
  },
  {
   "id": "tl-prologue-anchors",
   "text": "The named events on the heritage strip and in the heritage-band H2: Goodyear founded 1898, Semon plasticizes PVC 1926, the first standalone polymer department 1963, the NSF ALCOM center 1991, and 2012 as the last dated row. [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.] [2026-08-29: the 1963 label now points at H-007, the heritage row, not the merged-away discovery row D-003. A department is capacity, not a paper or a patent, which is this page's own definition of the two collections.]",
   "source": "heritage.json rows H-002, D-001, H-007, H-009e, D-009",
   "assert": "any(e['id'] == 'H-002' and e['year'] == 1898 and 'Goodyear' in e['title'] for e in D['h']['events']) and any(e['id'] == 'D-001' and e['year'] == 1926 and 'Semon' in e['title'] and 'PVC' in e['title'] for e in D['h']['events']) and any(e['id'] == 'H-007' and e['year'] == 1963 and 'polymer department' in e['title'] for e in D['h']['events']) and any(e['id'] == 'H-009e' and e['year'] == 1991 and 'ALCOM' in e['title'] for e in D['h']['events']) and len([e for e in D['h']['events'] if e['year'] == 2012]) == 1 and one(e for e in D['h']['events'] if e['year'] == 2012)['id'] == 'D-009'",
   "actual": "[(e['id'], e['year'], e['title']) for e in D['h']['events'] if e['id'] in ('H-002', 'D-001', 'H-007', 'H-009e', 'D-009')]",
   "falsified_if": "Any anchor row is relabeled, re-dated, moved between collections or dropped, or a second 2012 row appears (then 'the last dated row' label points at the wrong dot)."
  },
  {
   "id": "tl-cadence-before-after",
   "text": "The public record holds 4 events in the 34 months before the October 2023 federal designation and 67 delivered in the 34 months since; the spans match to within a day, so the record filled close to seventeen times as fast (hero standfirst, hero stat cards, pace-band H2 + lede + chart annotation + figure source line). [2026-08-28: the H2 said 'changed the cadence seventeenfold', which names the ratio without saying which way it moved and leads on a term of art; the plain reading now leads and 'seventeenfold' with its 68 divided by 4 arithmetic moved to the source line. The 17x assertion is unchanged.] [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json events, shown universe (PRE non-CTX rows plus in-window rows with show, as app.js builds it), split at the designation date; span arithmetic from the event dates and meta.asOf",
   "assert": "(lambda E, des, asof, jdn: (lambda before, since: len(before) == 4 and len(since) == 67 and round(len(since) / len(before)) == 17 and abs((jdn(asof) - jdn(des)) - (jdn(des) - jdn('2021-01-01'))) <= 2 and round((jdn(des) - jdn('2021-01-01')) / 30.44) == 34 and round((jdn(asof) - jdn(des)) / 30.44) == 34)([e for e in E if '2021-01-01' <= e['date'] < des and e.get('tier') != 'CTX' and e['delivered'] and (e['date'] < '2023-01-01' or e.get('show'))], [e for e in E if des <= e['date'] and e['delivered'] and e.get('show')]))(D['t']['events'], one(e['date'] for e in D['t']['events'] if e['date'].startswith('2023') and 'esignat' in e['title']), D['t']['meta']['asOf'], lambda s: (lambda y, m, d: (lambda a: d + (153 * (m + 12 * a - 3) + 2) // 5 + 365 * (y + 4800 - a) + (y + 4800 - a) // 4 - (y + 4800 - a) // 100 + (y + 4800 - a) // 400 - 32045)((14 - m) // 12))(int(s[:4]), int(s[5:7]), int(s[8:10])))",
   "actual": "(lambda E, des: (len([e for e in E if '2021-01-01' <= e['date'] < des and e.get('tier') != 'CTX' and e['delivered'] and (e['date'] < '2023-01-01' or e.get('show'))]), len([e for e in E if des <= e['date'] and e['delivered'] and e.get('show')])))(D['t']['events'], '2023-10-23')",
   "falsified_if": "A pre-designation public event is added to or dropped from the inventory (the 4 moves), a delivered event lands or is reclassified (the 68 moves and 17x stops being exact), or meta.asOf advances so the since-span no longer matches the before-span within a day \u2014 the '34 months either side' framing dies first. [2026-09-01: recomputed after one row was removed from the register. It recorded a named company's unsuccessful application, on a site whose README withholds two pages because naming an unsuccessful applicant is not something a funder gets to do. Two independent model families were asked to refute the decision to anonymise it instead of deleting it, and both refused: the row stayed identifiable because the same page names the company joining the cohort four months later, and keeping it to protect a count is letting the headline decide which sensitive records remain evidence. The count moved and the sentence moved with it.]"
  },
  {
   "id": "tl-cadence-per-year",
   "text": "Delivered public events per calendar year in the shown record: 2021: 1, 2022: 2, 2023: 4 (1 before the 23 October designation, 3 after), 2024: 14, 2025: 29, 2026 through 13 August: 21 (cadence chart bars and value labels).",
   "source": "timeline.json events, shown universe, delivered only, keyed by year",
   "assert": "(lambda R: [len([e for e in R if e['date'][:4] == y]) for y in ('2021', '2022', '2023', '2024', '2025', '2026')] == [1, 2, 4, 14, 29, 21] and len([e for e in R if e['date'][:4] == '2023' and e['date'] < '2023-10-23']) == 1)([e for e in D['t']['events'] if e['delivered'] and e.get('tier') != 'CTX' and (e['date'] < '2023-01-01' or e.get('show')) and e['date'] >= '2021-01-01'])",
   "actual": "(lambda R: sorted(set((e['date'][:4], len([x for x in R if x['date'][:4] == e['date'][:4]])) for e in R)))([e for e in D['t']['events'] if e['delivered'] and e.get('tier') != 'CTX' and (e['date'] < '2023-01-01' or e.get('show')) and e['date'] >= '2021-01-01'])",
   "falsified_if": "Any year's delivered count moves \u2014 a regeneration of the inventory, a visibility flip, or a new delivered event lands. The bars render from the data, but this pins the counts the prose and the aria summary state. [2026-09-01: recomputed after one row was removed from the register. It recorded a named company's unsuccessful application, on a site whose README withholds two pages because naming an unsuccessful applicant is not something a funder gets to do. Two independent model families were asked to refute the decision to anonymise it instead of deleting it, and both refused: the row stayed identifiable because the same page names the company joining the cohort four months later, and keeping it to protect a count is letting the headline decide which sensitive records remain evidence. The count moved and the sentence moved with it.]"
  },
  {
   "id": "tl-cadence-scope",
   "text": "The seventeenfold pace is a ratio between two FILTERED counts, and the source line now names the filter. It counts delivered rows the inventory marks public and shows, which leaves out 12 dated rows it files as media coverage (press and social mentions) and 2 rows of pre-1920 context. All 12 media rows fall after the designation, so counting them would put the ratio at 20 times rather than 17: the exclusion is conservative and makes the page's own number smaller. The fragility runs the other way, and is stated with it: the before count is 4, so one more pre-designation row takes the ratio to 13.6.",
   "source": "timeline.json: tier and show fields on all 104 rows, against the same 34-month spans the pace band uses",
   "assert": "(lambda M, C2: len(M) == 12 and all(not m['show'] and m['delivered'] for m in M) and all(m['date'] >= '2023-10-23' for m in M) and len(C2) == 2 and round((68 + len(M)) / 4, 1) == 20.0 and round(68 / (4 + 1), 1) == 13.6)([e for e in D['t']['events'] if e['tier'] == 'MEDIA'], [e for e in D['t']['events'] if e['tier'] == 'CTX'])",
   "actual": "{'media': [(e['date'], e['id']) for e in D['t']['events'] if e['tier'] == 'MEDIA'], 'ctx': [e['id'] for e in D['t']['events'] if e['tier'] == 'CTX']}",
   "falsified_if": "A media row is dated BEFORE the designation, at which point counting them would no longer only widen the gap and the 'conservative' word comes out of the sentence; or a media row is promoted to a shown tier, which moves the 68 and the 17x together. This claim exists because the page published a ratio for months without saying which rows it was a ratio of, and an exclusion that flatters nobody is still an exclusion a reader is entitled to see."
  },
  {
   "id": "tl-shown-counts",
   "text": "Since January 2023 the calendar shows 68 delivered events and 18 scheduled, 86 in all (calendar-band H2, closer sub, archive-band lede); the 87 is the sum, never typed independently. 67 of the 68 fall on or after the 23 October 2023 designation, and the difference is the single row dated 9 January 2023 (calendar-band lede, methodology). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.] [2026-08-29: a reader hit 68 in the pace band, 69 in the calendar band and 69 again in the closer, and could only reconcile them by adding up the bar chart. Both counts were typed into the markup. They are the same rows measured from two different start dates, the two start dates are now named on the page, and the row that separates them is named too. The assertion holds the relationship, not just the two numbers.]",
   "source": "timeline.json events, in-window shown universe as app.js builds it",
   "assert": "(lambda ins, des: len([e for e in ins if e['delivered']]) == 68 and len([e for e in ins if not e['delivered']]) == 18 and len(ins) == len([e for e in ins if e['delivered']]) + len([e for e in ins if not e['delivered']]) and len(ins) == 86 and len([e for e in ins if e['delivered'] and e['date'] >= des]) == 67 and len([e for e in ins if e['delivered'] and e['date'] < des]) == 1 and one(e for e in ins if e['delivered'] and e['date'] < des)['date'] == '2023-01-09')([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and (e.get('show') or not e['delivered'])], '2023-10-23')",
   "actual": "(lambda ins: (len([e for e in ins if e['delivered']]), len([e for e in ins if not e['delivered']]), len(ins), [(e['id'], e['date'], e['title']) for e in ins if e['delivered'] and e['date'] < '2023-10-23']))([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and (e.get('show') or not e['delivered'])])",
   "falsified_if": "The inventory adds a public event, a scheduled event is delivered (69/18 both move), the window constant in app.js moves off 2023-01-01, or a second row lands between the window opening and the designation. That last one is the dangerous case: the two counts would then differ by two and the page's reconciliation sentence, which names one row, would be quietly wrong. [2026-09-01: recomputed after one row was removed from the register. It recorded a named company's unsuccessful application, on a site whose README withholds two pages because naming an unsuccessful applicant is not something a funder gets to do. Two independent model families were asked to refute the decision to anonymise it instead of deleting it, and both refused: the row stayed identifiable because the same page names the company joining the cohort four months later, and keeping it to protect a count is letting the headline decide which sensitive records remain evidence. The count moved and the sentence moved with it.]"
  },
  {
   "id": "tl-sched-split",
   "text": "Of the 18 scheduled dates, 10 come due by the end of 2026; the other 8 land between 2027 and 2029: the pilot facility's readiness target and seven award or project end dates (promises-band lede, closer, swimlane on-chart takeaway). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json forward events by date and title",
   "assert": "(lambda sch: len(sch) == 18 and len([e for e in sch if e['date'] < '2027-01-01']) == 10 and (lambda far: len(far) == 8 and all('2027-01-01' <= e['date'] <= '2029-12-31' for e in far) and len([e for e in far if 'ready' in e['title'].lower()]) == 1 and len([e for e in far if 'end' in e['title'].lower() or 'completion' in e['title'].lower()]) == 7)([e for e in sch if e['date'] >= '2027-01-01']))([e for e in D['t']['events'] if not e['delivered'] and e['date'] >= '2023-01-01'])",
   "actual": "[(e['date'], e['title']) for e in D['t']['events'] if not e['delivered'] and e['date'] >= '2027-01-01']",
   "falsified_if": "A scheduled date is delivered or slips across the 2026/2027 boundary, a new forward milestone is published, or a 2027+ scheduled event appears that is neither the pilot-facility readiness target nor an end/completion date \u2014 the 'the other eight are completion dates' sentence would then be wrong."
  },
  {
   "id": "tl-rd-clocks",
   "text": "The five company R&D awards began in December 2024 and run to end dates between November 2027 and November 2029; BioVerde's $11.1M award is the largest (promises-band lede, closer, BioVerde aside, Dec-2024 rule label). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json: the five 'EDA award to X begins' events of Dec 2024 and the three forward award-period-end events F30/F37/F44",
   "assert": "(lambda dec: len(dec) == 5 and max(int(e['title'].split('$')[1].split(',')[0] + e['title'].split('$')[1].split(',')[1] + e['title'].split('$')[1].split(',')[2]) if e['title'].count(',') >= 2 else 0 for e in dec) == 11122386 and any('BioVerde' in e['title'] and '11,122,386' in e['title'] for e in dec))([e for e in D['t']['events'] if e['laneKey'] == 'rd' and e['delivered'] and e['date'][:7] == '2024-12' and 'EDA award' in e['title']]) and (lambda ends: sorted(e['date'] for e in ends) == ['2027-11-30', '2028-12-14', '2029-11-30'] and all('end' in e['title'].lower() for e in ends))([e for e in D['t']['events'] if e['laneKey'] == 'rd' and not e['delivered'] and e['date'] >= '2027-01-01'])",
   "actual": "([(e['date'], e['title']) for e in D['t']['events'] if e['laneKey'] == 'rd' and e['delivered'] and e['date'][:7] == '2024-12'], [(e['date'], e['title']) for e in D['t']['events'] if e['laneKey'] == 'rd' and not e['delivered'] and e['date'] >= '2027-01-01'])",
   "falsified_if": "An award start is re-dated, a sixth company R&D award begins, an award amount is corrected (the 'largest' ranking can flip), or any of the three end dates moves \u2014 each would rewrite the November 2027 to November 2029 window."
  },
  {
   "id": "tl-swimlane-rules",
   "text": "The labeled marks on the swimlane: the 23 Oct 2023 designation rule, drawn as 'one of 31 nationwide' because the inventory's own '1 of 31' reads as a rank, the 2 Jul 2024 ~$51M implementation-grant rule, the Dec 2024 five-award rule, the 14 Jul 2026 NSF NEO-SMART award leader labeled on its own dot, and the pilot-facility readiness leader. Both leader labels are built from the event they point at rather than typed. [2026-08-28: the figure subtitle used to say the rules mark three events while a fourth labeled line was drawn; it now says the three rules and names the NSF label separately, which is what the chart does.] [2026-08-29: the NSF label carried '$160M'. That figure existed nowhere in this repository except inside E205's typed title: no amount field, no source, no note, and an order of magnitude away from the $14,999,983 the federal money page reports for the same award from a federal record. It is REMOVED pending a source. The event, the date and the awarder stay; the calendar band's source line points at the figure that is sourced and does not assert a relationship between the two. The assertion now pins the ABSENCE, because a number this page cannot source must not come back by way of a rebuild. The pilot label also said 'Nov 2027' while the record said Oct-Dec 2027; both labels are now derived.] [2026-08-29, later the same day: the figure came back SOURCED. The NSF award record (2532460) gives an estimated total of $14,999,983 and $7,499,984 obligated for FY2026, and that total matches the federal money page's USAspending figure to the dollar. The amount is now a FIELD on the row, with its basis, its award id and its record, and every sentence and label that prints it is assembled from those fields. The program's ten-year ceiling is still not printed anywhere, in either direction: no source here ties it to this award, and reading the ceiling as the award is the exact conflation that produced the original error.]",
   "source": "timeline.json events E009, E022, E036\u2013E041, E205, F29. E205's amount fields come from NSF award record 2532460 (api.nsf.gov/services/v1/awards.json, retrieved 29 August 2026); federal-money/data/federal.json meta.excludes carries the same total from USAspending.",
   "assert": "any(e['date'] == '2023-10-23' and 'esignat' in e['title'] and '1 of 31' in e['title'] for e in D['t']['events']) and any(e['date'] == '2024-07-02' and '51M' in e['title'] for e in D['t']['events']) and len([e for e in D['t']['events'] if e['laneKey'] == 'rd' and e['delivered'] and e['date'][:7] == '2024-12' and 'EDA award' in e['title']]) == 5 and (lambda n: n['date'] == '2026-07-14' and 'NSF' in n['title'] and 'NEO-SMART' in n['title'] and '$' not in n['title'] and n['amount'] == 14999983 and n['obligated'] == 7499984 and n['obligated'] < n['amount'] and n['awardId'] == '2532460' and n['awardDate'] == '2026-07-13' and n['periodEnd'] == '2036-07-31' and n['amountBasis'] and n['obligatedBasis'] and '2532460' in n['amountSource'] and 'nsf.gov' in n['amountSource'] and str(n['amount']) in n['amountSource'].replace(',', ''))(one(e for e in D['t']['events'] if e['id'] == 'E205')) and not any('160' in str(e.get('amount', '')) + e['title'] + str(e.get('amountNote', '')) for e in D['t']['events']) and any(e['id'] == 'F29' and e['date'][:7] == '2027-11' and 'ready' in e['title'].lower() and not e['delivered'] for e in D['t']['events'])",
   "actual": "[(e['id'], e['date'], e['title'], e.get('amount'), e.get('obligated'), e.get('awardId')) for e in D['t']['events'] if e.get('id') in ('E009', 'E022', 'E205', 'F29')]",
   "falsified_if": "The NSF award's figure moves off its record, an amount appears on any row without a basis and a source, the obligation exceeds the total, or '160' turns up in an amount, a title or an amount note anywhere in the inventory. That last one is the specific guard: the original error was a ten-year program ceiling printed as this award's value, and it must not come back through a rebuild or through a caveat that repeats the number in order to deny it. If a source ever does tie a ceiling to this award, it belongs on the row as its own field with its own basis, not inside a sentence."
  },
  {
   "id": "tl-watch-next",
   "text": "The next three scheduled dates after 13 August 2026: 2026 Round 1 technical reviews conclude (15 Aug 2026), the PC3 annual conference (9 Sep 2026), and the pilot-facility groundbreaking target (9 Oct 2026) (watch list; the list itself renders from data).",
   "source": "timeline.json forward events sorted by date after meta.asOf",
   "assert": "(lambda nxt: [e['date'] for e in nxt] == ['2026-08-15', '2026-09-09', '2026-10-09'] and 'reviews conclude' in nxt[0]['title'] and 'PC3' in nxt[1]['title'] and 'groundbreaking' in nxt[2]['title'].lower())(sorted([e for e in D['t']['events'] if not e['delivered'] and e['date'] > D['t']['meta']['asOf']], key=lambda e: e['date'])[:3])",
   "actual": "[(e['date'], e['title']) for e in sorted([e for e in D['t']['events'] if not e['delivered'] and e['date'] > D['t']['meta']['asOf']], key=lambda e: e['date'])[:5]]",
   "falsified_if": "A nearer milestone is published, one of the three is delivered or re-dated, or asOf advances past 15 August 2026 \u2014 the watch list re-renders from data, and this claim forces the surrounding prose to move with it."
  },
  {
   "id": "tl-aside-bioverde",
   "text": "In September 2024 BioVerde CEO Dave Witte announced the company would move the bulk of its operations to Ohio, citing the grant; its $11.1M bio-based butadiene award began that December, the largest of the five company R&D awards (aside).",
   "source": "timeline.json events E028 and E040",
   "assert": "any(e['date'] == '2024-09-05' and 'BioVerde' in e['title'] and 'Witte' in e['title'] and 'moving' in e['title'] and 'citing the grant' in e['title'] for e in D['t']['events']) and any(e['date'] == '2024-12-15' and 'BioVerde' in e['title'] and 'Butadiene' in e['title'] and '11,122,386' in e['title'] for e in D['t']['events'])",
   "actual": "[(e['date'], e['title']) for e in D['t']['events'] if 'BioVerde' in e['title']]",
   "falsified_if": "Either BioVerde event is re-dated or reworded in the inventory, or the award amount is corrected \u2014 'largest' is separately pinned by tl-rd-clocks."
  },
  {
   "id": "tl-aside-barberton",
   "text": "Sixteen days after the designation, NovationSi and RD Abbott ran a polymer workshop at Barberton High School; fourteen months later Barberton launched a K-12 Polymer Pathway curriculum, backed by its community foundation (aside).",
   "source": "timeline.json events E013 and E048; day and month arithmetic from the recorded dates",
   "assert": "(lambda jdn: (lambda w, p, des: w['date'] == '2023-11-08' and jdn(w['date']) - jdn(des) == 16 and p['date'] == '2025-01-14' and 'K-12 Polymer Pathway' in p['title'] and 'Barberton Community Foundation' in p['org'] and round((jdn(p['date']) - jdn(w['date'])) / 30.44) == 14)(one(e for e in D['t']['events'] if 'Barberton HS' in e['title']), one(e for e in D['t']['events'] if 'K-12 Polymer Pathway' in e['title']), one(e['date'] for e in D['t']['events'] if e['date'].startswith('2023') and 'esignat' in e['title'])))(lambda s: (lambda y, m, d: (lambda a: d + (153 * (m + 12 * a - 3) + 2) // 5 + 365 * (y + 4800 - a) + (y + 4800 - a) // 4 - (y + 4800 - a) // 100 + (y + 4800 - a) // 400 - 32045)((14 - m) // 12))(int(s[:4]), int(s[5:7]), int(s[8:10])))",
   "actual": "[(e['date'], e['title'], e['org']) for e in D['t']['events'] if 'Barberton' in e['title'] or 'Barberton' in e['org']]",
   "falsified_if": "Either Barberton event is re-dated (the sixteen-days and fourteen-months arithmetic breaks), or the pathway's organization changes."
  },
  {
   "id": "tl-table-default",
   "text": "The table opens on the 12 most recent delivered events, newest first; expanding adds the 18 scheduled rows, tagged. All default rows are delivered, which is why the status column moved into the caption (archive-band lede, caption, disclosure button; claims describe the default, unfiltered state). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json in-window shown universe; the cap and ordering are app.js buildTable()",
   "assert": "(lambda ins: len([e for e in ins if e['delivered']]) >= 12 and len([e for e in ins if not e['delivered']]) == 18)([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and (e.get('show') or not e['delivered'])])",
   "actual": "(lambda ins: (len([e for e in ins if e['delivered']]), len([e for e in ins if not e['delivered']])))([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and (e.get('show') or not e['delivered'])])",
   "falsified_if": "Fewer than 12 delivered events exist (the cap sentence stops being true) or the scheduled count moves off 18 while the table sub still states it."
  },
  {
   "id": "tl-operating-five-workstreams",
   "text": "Five workstreams (calendar-band H2). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json lanes",
   "assert": "len(D['t']['lanes']) == 5",
   "actual": "[l['name'] for l in D['t']['lanes']]",
   "falsified_if": "A lane is added or merged in the inventory's LANES map."
  },
  {
   "id": "tl-operating-since-2023",
   "text": "Everything the cluster has publicly done since the 2023 federal designation. (calendar-band lede.) [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json: the 2023 designation is a public event in the record and the swimlane window opens at 2023-01-01",
   "assert": "any(e['date'].startswith('2023') and 'esignat' in e['title'] for e in D['t']['events']) and D['t']['meta']['counts']['events'] == len(D['t']['events'])",
   "actual": "[ (e['date'], e['title']) for e in D['t']['events'] if e['date'].startswith('2023') and 'esignat' in e['title'] ]",
   "falsified_if": "The designation event leaves the public record, or the window constant in app.js moves off 2023 without this sentence moving with it."
  },
  {
   "id": "tl-operating-as-of",
   "text": "Operating record as of 13 August 2026; heritage register as of 14 August 2026. The operating date is the mast dateline, stamped from the fetched date passed to PV.methodology(); the register date is stated in the methodology box Data sources. [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json meta.asOf; heritage.json meta.asOf",
   "assert": "D['t']['meta']['asOf'] == '2026-08-13' and D['h']['meta']['asOf'] == '2026-08-14'",
   "actual": "(D['t']['meta']['asOf'], D['h']['meta']['asOf'])",
   "falsified_if": "Either builder is re-run against a newer inventory or register: the fetched date passed to PV.methodology(), the NOW constants in app.js and the Today labels are static and must be edited with the data."
  },
  {
   "id": "tl-discoveries-not-products",
   "text": "A discovery is a paper or a patent, never a product.",
   "source": "NEO-POLYMER-WINS GOAL.md and collections/discoveries.md: 'A paper or a patent is enough. A product is a child row.'",
   "verify": "manual",
   "assert": null,
   "actual": null,
   "falsified_if": "The register redefines the discoveries collection to admit products, or a shipped/licensed row is emitted into the prologue (the builder drops those collections and counts the drops)."
  },
  {
   "id": "tl-heritage-not-cluster-outcome",
   "text": "None of it is a cluster outcome; it is the reason the cluster is here.",
   "source": "NEO-POLYMER-WINS GOAL.md: heritage and discoveries 'may not fill an EDA success cell'; every atom carries pic_caused: no",
   "verify": "manual",
   "assert": null,
   "actual": null,
   "falsified_if": "A heritage or discovery atom is ever marked pic_caused: yes. The register's own rule is that PIC-era awards live in a different collection and are not wins until they ship."
  },
  {
   "id": "tl-photos-public-domain",
   "text": "Five photographs, all in the public domain: four US federal works from the Library of Congress and the Naval History and Heritage Command, one from NASA. The photo band names them: the USS Akron under construction in 1930, three 1941 photographs of wartime rubber and fuel-tank work, and the NASA Glenn aerogel (photo-band lede and source line). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "NEO-POLYMER-WINS media/MEDIA-LEDGER.md and incoming/wave1b-media.md \u2014 LOC fsac.1a35066, fsac.1a35064, fsac.1a35068 (Palmer, FSA/OWI, PD); NHHC NH 42164 (US Navy, PD); NASA aerogels feature (NASA, PD)",
   "verify": "manual",
   "assert": null,
   "actual": null,
   "falsified_if": "Any of the five archive records is found to carry a third-party credit or a restriction. The ledger's rule stands: news-site, Getty, postcard and Wikipedia-page-as-licence images never ship, and the megadeck's vintage Airdock postcard stays out until its rights are checked."
  },
  {
   "id": "tl-photos-no-university-firsts",
   "text": "No archive on this page holds a cleared photograph of the university firsts, so none is shown. (photo-band source line.) [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "MEDIA-LEDGER.md: 'No public-domain photo found yet for UA 1909, Baer 1963, Kent LCI, Kennedy, Harris\u2013Cheng, or Dhinojwala. Do not fill those holes with news or campus PR.'",
   "verify": "manual",
   "assert": null,
   "actual": null,
   "falsified_if": "A university grants written permission for a science photograph or a public-domain one is found; then a frame may be added and this sentence must change."
  },
  {
   "id": "tl-pipeline-reconciliation",
   "text": "487 raw rows reduced to 103 dated events. Of those 103, 86 fall inside the 2023 to 2029 calendar window and are drawn there (68 delivered, 18 scheduled), 5 predate the window (3 of them dated 2021 or later and drawn in the cadence chart, 2 century-scale context rows counted nowhere), and the remaining 12 are press coverage of the cluster rather than things the cluster did (methodology box, Data sources). [2026-08-28: the methods block said '487 raw rows reconciled to 269' while the sources block said '487 raw rows reduced to 104 dated events', and neither tied to the 87 dots a reader can count. One basis now, and the split is computed at load time rather than typed.] [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.] [2026-08-29: the methods paragraph said all 5 pre-window rows feed the year-by-year chart. Three do. The chart starts at 2021 and drops the two context rows, so a reader counting its bars came up two short of a number the apparatus had given them. The split is computed now, not asserted.]",
   "source": "web/timeline/data/timeline.json meta.source and the events array; _data/SOURCES.json public_record.filters.compiled states the same 104",
   "assert": "'103 dated events' in D['t']['meta']['source'] and '269' not in D['t']['meta']['source'] and len(D['t']['events']) == 103 and D['t']['meta']['counts']['events'] == 103 and (lambda cal, pre, media: len(cal) == 86 and len(pre) == 5 and len(media) == 12 and len(cal) + len(pre) + len(media) == len(D['t']['events']) and all(e['tier'] == 'MEDIA' for e in media) and len([e for e in cal if e['delivered']]) == 68 and len([e for e in cal if not e['delivered']]) == 18)([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and (e.get('show') or not e['delivered'])], [e for e in D['t']['events'] if e['date'] < '2023-01-01'], [e for e in D['t']['events'] if e['date'] >= '2023-01-01' and e['delivered'] and not e.get('show')]) and len([e for e in D['t']['events'] if e['date'] < '2023-01-01' and e.get('tier') != 'CTX' and e['delivered']]) == 3 and len([e for e in D['t']['events'] if e['date'] < '2023-01-01' and e.get('tier') == 'CTX']) == 2 and all(e['date'] >= '2021-01-01' for e in D['t']['events'] if e['date'] < '2023-01-01' and e.get('tier') != 'CTX') and all(e['date'] < '2021-01-01' for e in D['t']['events'] if e['date'] < '2023-01-01' and e.get('tier') == 'CTX')",
   "actual": "(len(D['t']['events']), len([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and (e.get('show') or not e['delivered'])]), len([e for e in D['t']['events'] if e['date'] < '2023-01-01']), len([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and e['delivered'] and not e.get('show')]), sorted(set(e['tier'] for e in D['t']['events'] if e['date'] >= '2023-01-01' and e['delivered'] and not e.get('show'))))",
   "falsified_if": "The two apparatus blocks state the pipeline as two different numbers again, the 104 stops matching the shipped row count, the three parts stop summing to the whole, or a held-out row turns out to be something other than a MEDIA-tier coverage row. Any of those means a reader auditing the trust apparatus hits a contradiction inside it. It also breaks if a pre-window row's tier changes or one lands outside the year range the cadence chart draws, which is what made the 5 and the 3 bars disagree. [2026-09-01: recomputed after one row was removed from the register. It recorded a named company's unsuccessful application, on a site whose README withholds two pages because naming an unsuccessful applicant is not something a funder gets to do. Two independent model families were asked to refute the decision to anonymise it instead of deleting it, and both refused: the row stayed identifiable because the same page names the company joining the cohort four months later, and keeping it to protect a count is letting the headline decide which sensitive records remain evidence. The count moved and the sentence moved with it.]"
  },
  {
   "id": "tl-cadence-2026-partial",
   "text": "The 2026 bar counts 1 January to 13 August 2026, 21 delivered events, and is drawn lighter than the full years beside it (cadence chart, figure subtitle, methodology box). [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json meta.asOf and the delivered rows dated in 2026",
   "assert": "D['t']['meta']['asOf'] == '2026-08-13' and max(e['date'] for e in D['t']['events'] if e['delivered']) <= '2026-08-13' and len([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and (e.get('show') or not e['delivered']) and e['delivered'] and e['date'][:4] == '2026']) == 21",
   "actual": "(D['t']['meta']['asOf'], max(e['date'] for e in D['t']['events'] if e['delivered']), len([e for e in D['t']['events'] if e['date'] >= '2023-01-01' and (e.get('show') or not e['delivered']) and e['delivered'] and e['date'][:4] == '2026']))",
   "falsified_if": "A delivered event is dated after the as-of date, or the 2026 count moves. A partial year drawn at the same weight as a complete one reads as a complete one, so the lightening has to track the real cut-off."
  },
  {
   "id": "tl-date-display-one-family",
   "text": "The date column prints one family of formats: a day, a month, a year, or a spelled range such as 1\u201315 Aug 2026 (record table, heritage table, event panel, methodology box). [2026-08-28: five rows printed raw ISO or quarter strings, so one column mixed '2026-08-01 \u2192 08-15' with '22 Jul 2026'.] [2026-08-28: page migrated onto the house anatomy (mast / hero / bands / generated methodology box / closer); the sentence moved with its section and the assertion is unchanged.]",
   "source": "timeline.json dateDisplay on every row whose precision is coarser than a month; day and month rows are formatted at render time",
   "assert": "all(('-' not in e['dateDisplay'] and 'Q' not in e['dateDisplay'] and '/' not in e['dateDisplay'] and '\\u2192' not in e['dateDisplay']) for e in D['t']['events'] if e['datePrecision'] not in ('day', 'month'))",
   "actual": "sorted(set(e['dateDisplay'] for e in D['t']['events'] if e['datePrecision'] not in ('day', 'month')))",
   "falsified_if": "A rebuild reintroduces an ISO or quarter string into a rendered date cell. The CSV export still carries the machine form on purpose; this claim governs only what a reader sees."
  },
  {
   "id": "tl-footer-rebuilds-from-data",
   "text": "Every figure here rebuilds from the data files published beside this page (page footer, byline data credit).",
   "source": "web/timeline/data/timeline.json and web/timeline/data/heritage.json, the only two files the page reads",
   "assert": "len(D['t']['events']) > 0 and len(D['h']['events']) > 0 and all(e.get('date') for e in D['t']['events']) and all(e.get('date') for e in D['h']['events']) and 'Merged from eight' in D['t']['meta']['source'] and 'NEO Polymer Wins' in D['h']['meta']['source']",
   "actual": "(len(D['t']['events']), len(D['h']['events']))",
   "falsified_if": "Either data file empties, a row loses its date, or the two named sources in the byline stop being the sources the files declare. The footer promises a reader that the charts are not hand-drawn; if a value ever moves into the markup this claim stops covering it and the promise is false."
  },
  {
   "id": "tl-range-rows-sort-on-shown-date",
   "text": "A row recorded as a span sorts on the first date it displays, so the newest-first table's order matches the dates in its own date column (archive-band lede, record table). [2026-08-29: NEW. Two rows were anchored mid-year while displaying a later period: '1-15 Aug 2026' sat below '7 Jul 2026' in a table captioned 'Newest first', and 'Dec 2025 to early 2026' sat among mid-2025 rows. The sort key was the anchor date and the reader sees the display date, and nothing held the two together.]",
   "source": "timeline.json: for every row whose precision is a span, the anchor date the table sorts on must be the first year the row's own dateDisplay names",
   "assert": "(lambda E, firstYear: all(e['date'][:4] == firstYear(e['dateDisplay']) for e in E if e['datePrecision'] in ('era', 'range', 'quarter')) and one(e for e in E if e['id'] == 'E218')['date'] == '2026-08-01' and one(e for e in E if e['id'] == 'E218')['dateDisplay'] == '1\u201315 Aug 2026' and one(e for e in E if e['id'] == 'E127')['date'] == '2025-12-01' and one(e for e in E if e['id'] == 'E127')['dateDisplay'] == 'Dec 2025 to early 2026')(D['t']['events'], (lambda s: min(s[i:i + 4] for i in range(len(s) - 3) if s[i:i + 4].isdigit())))",
   "actual": "(lambda firstYear: [(e['id'], e['date'], e['dateDisplay'], firstYear(e['dateDisplay'])) for e in D['t']['events'] if e['datePrecision'] in ('era', 'range', 'quarter')])((lambda s: min(s[i:i + 4] for i in range(len(s) - 3) if s[i:i + 4].isdigit())))",
   "falsified_if": "A span row is anchored outside the period it prints. The table sorts on the anchor and the reader reads the display, so the moment those two disagree the caption 'Newest first' is false about a row a reader can point at."
  },
  {
   "id": "tl-projection-has-a-denominator",
   "text": "The register's one projection row is recorded with its scale, never printed and never endorsed. E023 carries a PIC op-ed's $5B impact and 4,000 jobs; it is a MEDIA row, so no figure from it reaches the page. On the row itself it now carries what the op-ed does not state (geography, industry scope, horizon) and one sourced denominator: the Akron OH MSA's entire plastics and rubber products sector, NAICS 326, employed 6,910 across 117 establishments in the 2024 annual average, which is the peers page's own BLS QCEW figure. The projected 4,000 is close to three fifths of that existing base.",
   "source": "timeline.json E023 claim fields, against peers/data/peers.json metros.326 subject (Akron, OH MSA, C1042, 2024)",
   "assert": "(lambda e: e['tier'] == 'MEDIA' and e['show'] is False and all(k in e for k in ('claim', 'claimBasis', 'claimContext', 'claimSource', 'claimNote')) and '6,910' in e['claimContext'] and 'not as agreement' in e['claimContext'] and 'not endorsing' in e['claimNote'])(one(e for e in D['t']['events'] if e['id'] == 'E023')) and not any(('5B' in e['title'] or '4,000' in e['title']) and e['show'] for e in D['t']['events'])",
   "actual": "{k: v for k, v in one(e for e in D['t']['events'] if e['id'] == 'E023').items() if k.startswith('claim') or k in ('tier', 'show')}",
   "falsified_if": "E023 is promoted to a shown tier, at which point a projection with no stated scope would be on the page and the sentence around it has to be written before it can be. Or the denominator moves: the QCEW figure is a 2024 annual average and a new vintage restates it, so the 'three fifths' is re-derived, never carried forward. This is the same discipline the NSF award figure got after it shipped a number with no record behind it, applied before the mistake instead of after."
  }
 ]
}
