{
  "data": "revisions.json",
  "claims": [
    {
      "id": "rev-share",
      "text": "About 95 percent of published months were revised at least once (hero card).",
      "source": "ALFRED archival vintages for three PPI series",
      "assert": "0.92 <= len(D['periods']) / len(set((r['series'], r['date']) for r in D['all'])) <= 0.98",
      "actual": "(len(D['periods']), len(set((r['series'], r['date']) for r in D['all'])))",
      "falsified_if": "A refetch adds recent months not yet revised, pulling the share down over time."
    },
    {
      "id": "rev-counts",
      "text": "259 of 273 published months were later changed; the other 14 have only one version on record (standfirst, chart-1 source line, closer).",
      "source": "count of periods with a later value, against every (series, month) in the vintage record",
      "assert": "len(D['periods']) == 259 and len(set((r['series'], r['date']) for r in D['all'])) == 273",
      "actual": "(len(D['periods']), len(set((r['series'], r['date']) for r in D['all'])), len(set((r['series'], r['date']) for r in D['all'])) - len(D['periods']))",
      "falsified_if": "Any refetch: a new reference month, or a first revision landing on one of the 14 unrevised months, moves either count. Both numbers are printed as exact counts in the standfirst, so either change makes the sentence wrong."
    },
    {
      "id": "rev-small",
      "text": "The typical revision is a fraction of a percent.",
      "source": "ALFRED first-published against latest value",
      "assert": "max(abs(p['pct']) for p in D['periods'] if p['pct'] is not None) < 2.0",
      "actual": "max(abs(p['pct']) for p in D['periods'] if p['pct'] is not None)",
      "falsified_if": "A benchmark revision moves any month by 2 percent or more. This claim licenses trusting the cost-scissors page; if it fails, that page needs a warning."
    },
    {
      "id": "rev-median",
      "text": "The typical revision is 0.15 percent, measured across the 259 months that were revised and not across all 273 published (standfirst, hero card, chart-2 title, the bracket drawn under chart 2, caveat box, closer).",
      "source": "median of the absolute percentage change, first print to latest, across all revised months",
      "assert": "0.145 <= (lambda a: (lambda s, n: s[n//2] if n % 2 else (s[n//2-1] + s[n//2]) / 2)(sorted(a), len(a)))([abs(p['pct']) for p in D['periods'] if p['pct'] is not None]) < 0.155",
      "actual": "(lambda a: (lambda s, n: s[n//2] if n % 2 else (s[n//2-1] + s[n//2]) / 2)(sorted(a), len(a)))([abs(p['pct']) for p in D['periods'] if p['pct'] is not None])",
      "falsified_if": "The median leaves the band that rounds to 0.15 percent, which would break the hero card, both chart titles and the axis bracket at once. The base is the other half of this claim: the card and the bracket said 'half of months' until 2026-08-28, which reads as all 273 and overstates the reassurance the page is offering."
    },
    {
      "id": "rev-vs-month",
      "text": "These prices move a median 0.34 percent from one month to the next, so a typical revision of 0.15 percent is a bit under half of an ordinary month's movement (hero card, closer, distribution table).",
      "source": "median absolute month-over-month change on the latest published values, over the same 270 consecutive-month pairs the reversal count is built from, against the median absolute revision",
      "assert": "(lambda A: (lambda PM, LV, MED: (lambda steps, rev: len(steps) == 270 and 0.335 <= MED(steps) < 0.345 and MED(steps) > 2 * MED(rev))([abs(LV(s, dt) / LV(s, PM(dt)) - 1) * 100 for s in sorted(set(r['series'] for r in A)) for dt in sorted(set(r['date'] for r in A if r['series'] == s)) if PM(dt) in set(r['date'] for r in A if r['series'] == s)], [abs(p['pct']) for p in D['periods'] if p['pct'] is not None]))(lambda dt: '%04d-%02d-01' % ((int(dt[:4]) - 1, 12) if dt[5:7] == '01' else (int(dt[:4]), int(dt[5:7]) - 1)), lambda s, dt: max([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt])[1], lambda a: (lambda t, n: t[n // 2] if n % 2 else (t[n // 2 - 1] + t[n // 2]) / 2)(sorted(a), len(a))))(D['all'])",
      "actual": "(lambda A: (lambda PM, LV, MED: (MED([abs(LV(s, dt) / LV(s, PM(dt)) - 1) * 100 for s in sorted(set(r['series'] for r in A)) for dt in sorted(set(r['date'] for r in A if r['series'] == s)) if PM(dt) in set(r['date'] for r in A if r['series'] == s)]), MED([abs(p['pct']) for p in D['periods'] if p['pct'] is not None])))(lambda dt: '%04d-%02d-01' % ((int(dt[:4]) - 1, 12) if dt[5:7] == '01' else (int(dt[:4]), int(dt[5:7]) - 1)), lambda s, dt: max([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt])[1], lambda a: (lambda t, n: t[n // 2] if n % 2 else (t[n // 2 - 1] + t[n // 2]) / 2)(sorted(a), len(a))))(D['all'])",
      "falsified_if": "The ordinary monthly move leaves the band that rounds to 0.34 percent, or it stops being more than twice the typical revision, at which point the yardstick reverses and a revision becomes comparable to a month's own movement. This is the page's only answer to 'is 0.15 percent small'; it had none until 2026-08-28 and its own limitations said so. Both quantities come from one archive, the same 270 pairs and the same three series, so a change in either side is a change in the finding rather than in the sources."
    },
    {
      "id": "rev-settle",
      "text": "No month changed more than six months after its first print, and 250 of the 259 revised months had stopped moving inside four (closer, distribution table).",
      "source": "for each revised month, the gap in months between the vintage that first published it and the vintage that last changed it",
      "assert": "(lambda lags: max(lags) == 6 and len([v for v in lags if v <= 4]) == 250 and len(lags) == 259)([(lambda a, b: (int(b[:4]) - int(a[:4])) * 12 + int(b[5:7]) - int(a[5:7]))(p['path'][0]['from'], p['path'][-1]['from']) for p in D['periods']])",
      "actual": "sorted(set((lambda a, b: (int(b[:4]) - int(a[:4])) * 12 + int(b[5:7]) - int(a[5:7]))(p['path'][0]['from'], p['path'][-1]['from']) for p in D['periods']))",
      "falsified_if": "Any month turns out to have moved more than six months after first print, or the count settled within four leaves 250. The closer tells a reader to price against the version published about half a year later, which is advice this claim alone supports; if a benchmark revision ever reaches further back than six months the advice is wrong and has to be withdrawn, not softened. The bound is what a 2019-2026 monthly archive shows, not a rule the BLS publishes, and the methodology box says so."
    },
    {
      "id": "rev-max",
      "text": "The largest single revision is 1.41 percent: industrial chemicals for September 2021, first published at 348.8 and now 343.866, a drop of 1.41 percent (chart-2 annotation, chart-1 panel annotation, chart-2 title, caveat box, vignette lede).",
      "source": "maximum absolute percentage change across all revised months, recomputed from the two index values the page prints beside it",
      "assert": "(lambda b: b['series'] == 'WPU06' and b['date'] == '2021-09-01' and b['first'] == 348.8 and b['latest'] == 343.866 and abs(b['pct']) == max(abs(p['pct']) for p in D['periods'] if p['pct'] is not None) and round(abs((b['latest'] - b['first']) / b['first'] * 100), 2) == 1.41)(max([p for p in D['periods'] if p['pct'] is not None], key=lambda p: abs(p['pct'])))",
      "actual": "(lambda b: (b['series'], b['date'], b['first'], b['latest'], b['pct'], round(abs((b['latest'] - b['first']) / b['first'] * 100), 2)))(max([p for p in D['periods'] if p['pct'] is not None], key=lambda p: abs(p['pct'])))",
      "falsified_if": "A later vintage moves September 2021 again, or any other month overtakes it. Five surfaces name this month, so a new record-holder invalidates the vignette, not just a number. The clause that matters most is the last one in the assertion: it recomputes the printed 1.41 from 348.8 and 343.866, the two values the page sets beside it, and fails if they ever stop reconciling. The page printed 1.42 until 2026-08-28 because the derived file rounds the change to three places (-1.415) and rounding that again to two gives 1.42, while a reader dividing the two index values gets 1.41."
    },
    {
      "id": "rev-lean",
      "text": "137 months were later revised up against 121 revised down, a split close enough to even that no direction shows in the first estimates (hero card, chart-2 subtitle, chart-2 arrow annotation, distribution lede).",
      "source": "sign of the total change, first print to latest, per revised month; and a two-sided binomial test of that split against an even one",
      "assert": "(lambda dn, up: (dn, up) == (121, 137) and (lambda n, k: sum(math.comb(n, i) for i in range(n + 1) if abs(i - n / 2) >= abs(k - n / 2)) / 2 ** n)(dn + up, up) > 0.05)(len([p for p in D['periods'] if p['pct'] is not None and p['pct'] < 0]), len([p for p in D['periods'] if p['pct'] is not None and p['pct'] > 0]))",
      "actual": "(lambda dn, up: (dn, up, (lambda n, k: sum(math.comb(n, i) for i in range(n + 1) if abs(i - n / 2) >= abs(k - n / 2)) / 2 ** n)(dn + up, up)))(len([p for p in D['periods'] if p['pct'] is not None and p['pct'] < 0]), len([p for p in D['periods'] if p['pct'] is not None and p['pct'] > 0]))",
      "falsified_if": "Either count changes, or the split becomes lopsided enough that a fair coin would produce it less than one time in twenty, at which point 'close to an even split' is the wrong reading and the card, the chart annotation and the section lede all have to say so instead. Until 2026-08-28 those three surfaces asserted the opposite, that first estimates run slightly low: 137 of 258 is 53.1 percent, eight months from even against a standard deviation of 8.0, and a two-sided binomial test puts it at p = 0.35."
    },
    {
      "id": "rev-net-zero",
      "text": "One revised month is neither up nor down: it came back to within 0.001 of where it started, which is why 137 up plus 121 down is 258 against 259 revised months (chart-2 subtitle in plain words, distribution table in index points).",
      "source": "revised months whose published percentage change rounds to zero, and the index-point gap between their first and latest values",
      "assert": "[(p['series'], p['date'], round(abs(p['latest'] - p['first']), 3)) for p in D['periods'] if p['pct'] == 0] == [('PCU326326', '2025-04-01', 0.001)] and len([p for p in D['periods'] if p['pct'] is not None and p['pct'] < 0]) + len([p for p in D['periods'] if p['pct'] is not None and p['pct'] > 0]) + 1 == len(D['periods'])",
      "actual": "[(p['series'], p['date'], p['first'], p['latest'], round(abs(p['latest'] - p['first']), 3)) for p in D['periods'] if p['pct'] == 0]",
      "falsified_if": "A refetch moves that month off zero, or gives a second month a change that rounds to zero, or changes the 0.001 gap. Any of those breaks the arithmetic the subtitle uses to reconcile the two annotated counts against the stated total, and the reconciliation would have to be rewritten rather than the number nudged."
    },
    {
      "id": "rev-asof",
      "text": "The newest archived vintage on this page is from August 2026, which is the date the byline carries (hero byline).",
      "source": "maximum vintage date across every archived observation",
      "assert": "max(r['vintage'] for r in D['all'])[:7] == '2026-08'",
      "actual": "max(r['vintage'] for r in D['all'])",
      "falsified_if": "A refetch pulls a newer vintage, which moves the byline date. The byline reads this maximum from the data rather than carrying a typed month, so the page cannot silently claim to be more current than its archive is; this claim fails loudly if that wiring is ever replaced by a literal."
    },
    {
      "id": "rev-interview-ask",
      "verify": "manual",
      "text": "Nobody was interviewed for this page, and the reporting it lacks is one call to a resin purchaser who quotes WPU06 in a contract (methodology, limitations).",
      "source": "The page's own reporting record: no interview was conducted, and no quotation from any person appears anywhere on it. The human-scale beat is the September 2021 board-deck translation in the vignette, a rung-3 concrete vignette.",
      "reviewer": "recorded as the page's open reporting ask rather than left as an unexplained absence: who to call, the three questions, and the vignette paragraph the quote would slot into are all named in the Limitations section",
      "falsified_if": "A quotation from a named person appears on the page while this note still says nobody was interviewed, or a resin purchaser is reached and says WPU06 revisions never touch their contract terms, which would remove the reason the ask is worth making and the sentence would have to go."
    },
    {
      "id": "rev-frequency",
      "text": "741 corrections were published across the 259 revised months, an average of 2.9 each and never more than 4 on one month (hero card, distribution table).",
      "source": "revision count per revised reference month, and its total",
      "assert": "sum(p['revisions'] for p in D['periods']) == 741 and 2.85 <= sum(p['revisions'] for p in D['periods']) / len(D['periods']) < 2.95 and max(p['revisions'] for p in D['periods']) == 4",
      "actual": "(sum(p['revisions'] for p in D['periods']), len(D['periods']), sum(p['revisions'] for p in D['periods']) / len(D['periods']), max(p['revisions'] for p in D['periods']))",
      "falsified_if": "The total moves, or the mean leaves the band that rounds to 2.9, or any month accumulates a fifth revision. The total is on the card so the average can be checked: 741 over 259 is 2.86, which is what 2.9 rounds from. The card carried the average with no denominator anywhere on the page until 2026-08-28, so it was the one headline figure a reader could not reproduce."
    },
    {
      "id": "rev-series-medians",
      "text": "Industrial chemicals moves a median 0.35 percent after publication, more than twice as far as either downstream series (0.14 and 0.10) (chart-1 title, panel subtitles, where each median is stated as the half of that series' months that moved less).",
      "source": "median absolute percentage change per series",
      "assert": "(lambda M: 0.345 <= M('WPU06') < 0.355 and 0.135 <= M('WPU072') < 0.145 and 0.095 <= M('PCU326326') < 0.105 and M('WPU06') > 2 * M('WPU072') and M('WPU06') > 2 * M('PCU326326'))(lambda s: (lambda a: (lambda t, n: t[n//2] if n % 2 else (t[n//2-1] + t[n//2]) / 2)(sorted(a), len(a)))([abs(p['pct']) for p in D['periods'] if p['series'] == s]))",
      "actual": "sorted([(s, (lambda a: (lambda t, n: t[n//2] if n % 2 else (t[n//2-1] + t[n//2]) / 2)(sorted(a), len(a)))([abs(p['pct']) for p in D['periods'] if p['series'] == s])) for s in set(p['series'] for p in D['periods'])])",
      "falsified_if": "Any of the three medians leaves the band that rounds to its printed value, or industrial chemicals stops being more than twice either of the others, which is the comparison the panel order encodes."
    },
    {
      "id": "rev-panel-extremes",
      "text": "The month annotated on each panel is that series' largest move: industrial chemicals September 2021, rubber and plastic products February 2023, plastics and rubber plants May 2021.",
      "source": "argmax of the absolute percentage change within each series",
      "assert": "sorted([(s, max([p for p in D['periods'] if p['series'] == s], key=lambda p: abs(p['pct']))['date']) for s in set(p['series'] for p in D['periods'])]) == [('PCU326326', '2021-05-01'), ('WPU06', '2021-09-01'), ('WPU072', '2023-02-01')]",
      "actual": "sorted([(s, max([p for p in D['periods'] if p['series'] == s], key=lambda p: abs(p['pct']))['date'], max([p for p in D['periods'] if p['series'] == s], key=lambda p: abs(p['pct']))['pct']) for s in set(p['series'] for p in D['periods'])])",
      "falsified_if": "A later vintage gives any series a bigger mover, which would move the drawn annotation to a different bar while the chart still claims to mark the extreme."
    },
    {
      "id": "rev-series-months",
      "text": "Each series contributes 91 published months, of which 89 (industrial chemicals), 86 (rubber and plastic products) and 84 (plastics and rubber plants) were revised (panel subtitles).",
      "source": "months present in the vintage record, and months carrying a later value, per series",
      "assert": "sorted([(s, len(set(r['date'] for r in D['all'] if r['series'] == s)), len([p for p in D['periods'] if p['series'] == s])) for s in set(r['series'] for r in D['all'])]) == [('PCU326326', 91, 84), ('WPU06', 91, 89), ('WPU072', 91, 86)]",
      "actual": "sorted([(s, len(set(r['date'] for r in D['all'] if r['series'] == s)), len([p for p in D['periods'] if p['series'] == s])) for s in set(r['series'] for r in D['all'])])",
      "falsified_if": "A refetch extends the span or revises one of the unrevised months, changing a subtitle printed under each panel."
    },
    {
      "id": "rev-span",
      "text": "The months being priced run January 2019 to July 2026, 91 of them (hero byline, chart-1 source line).",
      "source": "distinct reference months in the vintage record",
      "assert": "(min(r['date'] for r in D['all']), max(r['date'] for r in D['all']), len(set(r['date'] for r in D['all']))) == ('2019-01-01', '2026-07-01', 91)",
      "actual": "(min(r['date'] for r in D['all']), max(r['date'] for r in D['all']), len(set(r['date'] for r in D['all'])))",
      "falsified_if": "A refetch adds a month, which changes the period stated in the eyebrow and the shared x axis all three panels are drawn on."
    },
    {
      "id": "rev-mom-flips",
      "text": "Of 270 month-over-month pairs, 201 clear the 0.1 percent floor on both readings and 14 of those reversed direction between the first estimate and today (caveat box, vignette, closer, distribution table).",
      "source": "each month's step from the previous month as first published (previous month taken at the same vintage), against the same step on today's values",
      "assert": "(lambda A: (lambda PM, FV, LV, AS: (lambda pairs: (len(pairs), len([p for p in pairs if abs(p[0]) >= 0.1 and abs(p[1]) >= 0.1]), len([p for p in pairs if abs(p[0]) >= 0.1 and abs(p[1]) >= 0.1 and p[0] * p[1] < 0])) == (270, 201, 14))([((FV(s, dt)[1] / AS(s, PM(dt), FV(s, dt)[0]) - 1) * 100, (LV(s, dt) / LV(s, PM(dt)) - 1) * 100) for s in sorted(set(r['series'] for r in A)) for dt in sorted(set(r['date'] for r in A if r['series'] == s)) if PM(dt) in set(r['date'] for r in A if r['series'] == s) and AS(s, PM(dt), FV(s, dt)[0]) is not None]))(lambda dt: '%04d-%02d-01' % ((int(dt[:4]) - 1, 12) if dt[5:7] == '01' else (int(dt[:4]), int(dt[5:7]) - 1)), lambda s, dt: min([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt]), lambda s, dt: max([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt])[1], lambda s, dt, V: (lambda c: max(c)[1] if c else None)([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt and r['vintage'] <= V])))(D['all'])",
      "actual": "(lambda A: (lambda PM, FV, LV, AS: (lambda pairs: (len(pairs), len([p for p in pairs if abs(p[0]) >= 0.1 and abs(p[1]) >= 0.1]), len([p for p in pairs if abs(p[0]) >= 0.1 and abs(p[1]) >= 0.1 and p[0] * p[1] < 0])))([((FV(s, dt)[1] / AS(s, PM(dt), FV(s, dt)[0]) - 1) * 100, (LV(s, dt) / LV(s, PM(dt)) - 1) * 100) for s in sorted(set(r['series'] for r in A)) for dt in sorted(set(r['date'] for r in A if r['series'] == s)) if PM(dt) in set(r['date'] for r in A if r['series'] == s) and AS(s, PM(dt), FV(s, dt)[0]) is not None]))(lambda dt: '%04d-%02d-01' % ((int(dt[:4]) - 1, 12) if dt[5:7] == '01' else (int(dt[:4]), int(dt[5:7]) - 1)), lambda s, dt: min([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt]), lambda s, dt: max([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt])[1], lambda s, dt, V: (lambda c: max(c)[1] if c else None)([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt and r['vintage'] <= V])))(D['all'])",
      "falsified_if": "Any of the three counts moves. The floor is deliberate: without it 36 of 270 pairs flip, most of them between readings of two hundredths of a percent, and calling those reversals would overstate the finding. The claim fails if a refetch changes the panel, and it fails loudly if the floor is ever removed from the page code without being removed here."
    },
    {
      "id": "rev-flip-concentration",
      "text": "12 of the 14 reversals are industrial chemicals, the series that revises hardest (vignette).",
      "source": "series of each sign-changing month pair",
      "assert": "(lambda A: (lambda PM, FV, LV, AS: len([1 for s in sorted(set(r['series'] for r in A)) for dt in sorted(set(r['date'] for r in A if r['series'] == s)) if s == 'WPU06' and PM(dt) in set(r['date'] for r in A if r['series'] == s) and AS(s, PM(dt), FV(s, dt)[0]) is not None and abs((FV(s, dt)[1] / AS(s, PM(dt), FV(s, dt)[0]) - 1) * 100) >= 0.1 and abs((LV(s, dt) / LV(s, PM(dt)) - 1) * 100) >= 0.1 and (FV(s, dt)[1] / AS(s, PM(dt), FV(s, dt)[0]) - 1) * (LV(s, dt) / LV(s, PM(dt)) - 1) < 0]) == 12)(lambda dt: '%04d-%02d-01' % ((int(dt[:4]) - 1, 12) if dt[5:7] == '01' else (int(dt[:4]), int(dt[5:7]) - 1)), lambda s, dt: min([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt]), lambda s, dt: max([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt])[1], lambda s, dt, V: (lambda c: max(c)[1] if c else None)([(r['vintage'], r['value']) for r in A if r['series'] == s and r['date'] == dt and r['vintage'] <= V])))(D['all'])",
      "actual": "'recount the WPU06 share of sign changes'",
      "falsified_if": "The concentration in industrial chemicals breaks, which would remove the link between the size of a series' revisions and its tendency to reverse direction, and with it the reason the closer points this method at the employment series."
    },
    {
      "id": "rev-vignette-direction",
      "text": "September 2021 read 1.0 percent above August in the figures published that October, and reads 0.3 percent below August today (stat band).",
      "source": "WPU06 September 2021 against August 2021, at the 2021-10-14 vintage and at the latest",
      "assert": "(lambda A: (lambda G: (lambda then, now: 0.95 <= then < 1.05 and -0.35 < now <= -0.25)((G('2021-09-01', 'first') / G('2021-08-01', 'asof') - 1) * 100, (G('2021-09-01', 'latest') / G('2021-08-01', 'latest') - 1) * 100))(lambda dt, kind: (lambda v: v[0][1] if kind == 'first' else (v[-1][1] if kind == 'latest' else max([x for x in v if x[0] <= '2021-10-14'])[1]))(sorted([(r['vintage'], r['value']) for r in A if r['series'] == 'WPU06' and r['date'] == dt]))))(D['all'])",
      "actual": "(lambda A: (lambda G: ((G('2021-09-01', 'first') / G('2021-08-01', 'asof') - 1) * 100, (G('2021-09-01', 'latest') / G('2021-08-01', 'latest') - 1) * 100))(lambda dt, kind: (lambda v: v[0][1] if kind == 'first' else (v[-1][1] if kind == 'latest' else max([x for x in v if x[0] <= '2021-10-14'])[1]))(sorted([(r['vintage'], r['value']) for r in A if r['series'] == 'WPU06' and r['date'] == dt]))))(D['all'])",
      "falsified_if": "Either step leaves the band that rounds to its printed value, or the two stop having opposite signs, which is the whole point of the beat."
    },
    {
      "id": "qwi-restates-history",
      "verify": "manual",
      "text": "The Quarterly Workforce Indicators restate whole histories each time they are rebuilt, and how far they move is not measured on this page (closer).",
      "source": "Census LEHD QWI release documentation; the QWI series the churn page is built on (churn/data/churn.json meta.source). No archived QWI vintages are fetched anywhere in this repo, so nothing here can re-run this assertion.",
      "reviewer": "flagged as the page's open data need: archived QWI and county-employment vintages",
      "falsified_if": "A vintage-to-vintage comparison of QWI, once those vintages are fetched, shows revisions no larger than the three PPI series here. That would remove the reason the closer points this method at the employment series next, and the sentence would have to go."
    },
    {
      "id": "rev-vignette-year",
      "text": "Against September 2020 the first estimates showed industrial chemicals up 24.8 percent, and today the same comparison reads 23.1 percent (vignette lede).",
      "source": "WPU06 September 2021 against September 2020, at the 2021-10-14 vintage and at the latest",
      "assert": "(lambda A: (lambda G: 24.75 <= (G('2021-09-01', 'first') / G('2020-09-01', 'asof') - 1) * 100 < 24.85 and 23.05 <= (G('2021-09-01', 'latest') / G('2020-09-01', 'latest') - 1) * 100 < 23.15)(lambda dt, kind: (lambda v: v[0][1] if kind == 'first' else (v[-1][1] if kind == 'latest' else max([x for x in v if x[0] <= '2021-10-14'])[1]))(sorted([(r['vintage'], r['value']) for r in A if r['series'] == 'WPU06' and r['date'] == dt]))))(D['all'])",
      "actual": "(lambda A: (lambda G: ((G('2021-09-01', 'first') / G('2020-09-01', 'asof') - 1) * 100, (G('2021-09-01', 'latest') / G('2020-09-01', 'latest') - 1) * 100))(lambda dt, kind: (lambda v: v[0][1] if kind == 'first' else (v[-1][1] if kind == 'latest' else max([x for x in v if x[0] <= '2021-10-14'])[1]))(sorted([(r['vintage'], r['value']) for r in A if r['series'] == 'WPU06' and r['date'] == dt]))))(D['all'])",
      "falsified_if": "Either annual figure leaves the band that rounds to its printed value, or the gap between them widens enough that the annual story no longer survives the revision, which is the half of the vignette that says what the first print got right."
    }
  ]
}
