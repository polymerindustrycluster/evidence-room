{
  "data": "lq.json",
  "claims": [
    {
      "id": "lq-reproduces-bls",
      "text": "The computed location quotient reproduces BLS published figures to within 0.005.",
      "source": "derive_lq.py cross-check against lq_annual_avg_emplvl",
      "assert": "D['meta']['verification']['max_abs_residual'] <= 0.005 and D['meta']['verification']['cells_checked'] >= len(D['meta']['footprint']['counties']) * 6 * 8",
      "actual": "D['meta']['verification']",
      "falsified_if": "BLS changes its LQ denominator, or our own_code 0 denominator drifts. The cell-count floor scales with the footprint \u2014 it dropped from 726 to 667 when NEO-14 became PIC-12, which is arithmetic, not error.",
      "official_check": "This IS the official cross-check."
    },
    {
      "id": "lq-326-latest",
      "text": "PIC-12 plastics and rubber sits at 2.32x the national share in the latest year.",
      "source": "QCEW composite summed from 14 counties",
      "assert": "round(one(c for c in D['composite'] if c['naics']=='326' and c['year']==max(x['year'] for x in D['composite']))['lq'],2) == 2.32",
      "actual": "[(c['year'],c['lq']) for c in D['composite'] if c['naics']=='326'][-3:]",
      "falsified_if": "Any county disclosure flips; the composite excludes withheld counties and is a floor. Changed from 2.43 to 2.32 when the footprint moved from NEO-14 to PIC-12."
    },
    {
      "id": "lq-paint-highest",
      "text": "Paint, coating and adhesive is the strongest regional polymer concentration.",
      "source": "QCEW composite, latest year",
      "assert": "max((c for c in D['composite'] if c['year']==max(x['year'] for x in D['composite']) and c['lq']), key=lambda c: c['lq'])['naics'] == '3255'",
      "actual": "sorted([(c['naics'],c['lq']) for c in D['composite'] if c['year']==max(x['year'] for x in D['composite']) and c['lq']], key=lambda t:-t[1])[:3]",
      "falsified_if": "Another NAICS overtakes 3255, or its heavy suppression (7 of 14 counties) flips."
    },
    {
      "id": "lq-suppression",
      "text": "167 cells are withheld by BLS and carry no value.",
      "source": "QCEW disclosure_code across all years and counties",
      "assert": "len([c for c in D['cells'] if c['suppressed']]) == 167",
      "actual": "len([c for c in D['cells'] if c['suppressed']])",
      "falsified_if": "A refetch changes the disclosure pattern."
    },
    {
      "id": "lq-registers",
      "text": "NAICS 325 is labeled context, not cluster; 3252, 3255 and 326 are the measurement register.",
      "source": "Documents/NAICS work/PIC_NAICS_Crosswalk_ADDENDUM_2026-08-11.md, John's decision 2026-08-11",
      "assert": "sorted(n['code'] for n in D['naics'] if n['register']=='core') == ['3252','3255','326'] and one(n for n in D['naics'] if n['code']=='325')['register'] == 'context'",
      "actual": "[(n['code'], n['register']) for n in D['naics']]",
      "falsified_if": "PIC changes the measurement register, or someone adds a NAICS without classifying it. Using 325 as cluster overstates NEO-14 by 36.7 percent."
    },
    {
      "id": "lq-footprint",
      "text": "This page uses the PIC-12 footprint.",
      "source": "John's decision 2026-08-14 \u2014 federal-data pages adopt PIC-12",
      "assert": "D['meta']['footprint']['label'] == 'PIC-12' and D['meta']['footprint']['n'] == 12",
      "actual": "D['meta']['footprint']",
      "falsified_if": "FOOTPRINT: this page uses PIC-12, matching the cluster-health dashboard. It does NOT reconcile with the vault-sourced pages, which use NEO-14 \u2014 the two share only 10 counties."
    }
  ]
}